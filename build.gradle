allprojects {
   apply plugin: 'eclipse'
   apply plugin: 'idea'   
}

// Instructions for each sub project
subprojects {
   repositories {
        mavenCentral()
        maven { url 'http://repo.spring.io/release' }
   }
}